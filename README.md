Loads a large time-series dataset of page views.

Engineers features that reflect traffic behavior (averages, spikes, zeros, skewness, recent vs older period ratio).

Simulates a binary label “bot vs human” using heuristics.

Trains a Random Forest classifier on those features.

Evaluates its performance (metrics + confusion matrix).

Interprets which features matter most.

Demonstrates a prediction for a single example.
